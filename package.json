{
  "name": "hahasns",
  "version": "1.0.0",
  "private": true,
  "description": "SaotieSNS — 轻社交 · 轻论坛 · 轻社区, an original lightweight social-community platform (NestJS + React)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm install --no-audit --no-fund && npm --prefix server-nest install && npm --prefix client install",
    "dev": "concurrently -n api,client -c blue,magenta \"npm --prefix server-nest run start:dev\" \"npm --prefix client run dev\"",
    "check": "npm --prefix client run typecheck && npm --prefix server-nest test",
    "deploy:check": "node scripts/check-deploy.mjs",
    "db:migration:check": "node scripts/check-migrations.mjs",
    "db:migration:show": "bash scripts/migrate-prod.sh show",
    "db:migration:run": "bash scripts/migrate-prod.sh run",
    "db:migration:revert": "bash scripts/migrate-prod.sh revert",
    "predeploy": "npm run deploy:check && npm run db:migration:check && npm run check && npm run build",
    "backup": "bash scripts/backup.sh",
    "backup:cron": "bash scripts/install-backup-cron.sh",
    "deploy:prod": "bash scripts/deploy-prod.sh",
    "build": "npm --prefix client run build && npm --prefix server-nest run build",
    "start": "npm --prefix server-nest run start:prod",
    "clean": "npm run clean:generated",
    "clean:generated": "node scripts/clean-generated.mjs",
    "changelog:gen": "node scripts/gen-changelog.mjs",
    "lint:copy": "node scripts/lint-copy.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
